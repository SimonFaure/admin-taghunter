/**
 * Stub body — exists only to satisfy the ScenarioAdapter.Body contract for
 * the typecheck-only tracksStubAdapter.
 *
 * Slice 2A.
 */

export function TracksBody() {
  return (
    <section className="bg-white rounded-lg shadow-sm border border-dashed border-gray-300 p-4">
      <h2 className="text-base font-semibold text-gray-900 mb-2">Tracks (stub body)</h2>
      <p className="text-sm text-gray-500">
        Type-specific UI for the tracks game type lives here once the model is decided.
      </p>
    </section>
  );
}